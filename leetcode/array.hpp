#ifndef ARRAY_HPP
#define ARRAY_HPP

#include <vector>
#include <bits/stdc++.h>

class Solution {
public:
    bool containsDuplicate(std::vector<int>& nums);
    bool isAnagram(std::string s, std::string t);
};

#endif
